👤
You
Along with the title editing, I want users to be able to edit the stuff that the ai returns, any actionable item, recap, etc. This will be a similar pattern to the title.
🤖
Cursor Assistant
Let me start by understanding the current title editing pattern and the meeting detail page structure.
🤖
Cursor Assistant
Now let me read the full files I need to modify.
🤖
Cursor Assistant
Good, `class-transformer` is already installed. Let me now implement all the changes across the stack.

**Step 1: Update shared types** - expand `UpdateMeetingRequest` to accept all editable fields.
🤖
Cursor Assistant
**Step 2: Update API DTO** - add nested validation classes and expand `UpdateMeetingDto`.
🤖
Cursor Assistant
**Step 3: Update API service** - apply all provided fields in the update method.
🤖
Cursor Assistant
Now for the big part - updating the frontend. I'll rewrite the meeting detail page with the generalized action, reusable editable components, and wire everything together.
🤖
Cursor Assistant
Now I'll add the reusable `EditableText` and `EditableListSection` components, and also update the pencil icon into a small shared constant to avoid duplication.
🤖
Cursor Assistant
Now add the shared pencil icon and the new editable components right after the `EditableTitle` component.
🤖
Cursor Assistant
Now let me also refactor `EditableTitle` to use the shared `PencilIcon` / `EditButton`, and then wire up all sections in `MeetingDetail`.
🤖
Cursor Assistant
Now wire up all the sections in the `MeetingDetail` component to use the new editable components.
🤖
Cursor Assistant
Let me read the final state of the files and check for linter errors.
🤖
Cursor Assistant
The files look clean. But I notice a UX issue - in `EditableListSection`, the `onBlur` on the text input would trigger a save if the user clicks into the assignee field. Let me fix that by using a container-level blur check.
🤖
Cursor Assistant
Now let me also remove the `onMouseDown` stopPropagation on the assignee input since it's no longer needed - the blur is handled at the container level.
🤖
Cursor Assistant
All changes are complete. Here's a summary of what was done across the full stack:

### Shared types (`packages/shared/src/index.ts`)
- Expanded `UpdateMeetingRequest` from `{ title: string }` to include optional `summary`, `actionItems`, `decisions`, and `openQuestions` fields.

### API DTO (`apps/api/src/meetings/create-meeting.dto.ts`)
- Added `ActionItemDto`, `DecisionDto`, and `OpenQuestionDto` validation classes with proper `class-validator` decorators.
- Expanded `UpdateMeetingDto` to accept all fields as optional, with `@ValidateNested` + `@Type()` for the array fields.

### API Service (`apps/api/src/meetings/meetings.service.ts`)
- Updated the `update` method to conditionally apply each field only when present in the DTO (using `!== undefined` checks).

### Frontend (`apps/web/app/routes/meetings.$id.tsx`)
Three new reusable components following the same pattern as `EditableTitle`:

- **`EditableSummary`** — click-to-edit textarea for the summary, auto-resizes, saves on blur, cancels on Escape.
- **`EditableListSection<T>`** — generic editable list for action items, decisions, and open questions. Each item shows a pencil icon on hover; clicking it opens an inline text input. Supports extra fields (like the assignee on action items) via `renderExtra`/`renderExtraEditor` props. Saves the full updated array on edit. Uses container-level `onBlur` so tabbing between the text and assignee inputs doesn't prematurely trigger a save.
- **`EditButton` / `PencilIcon`** — shared small components to deduplicate the edit icon across `EditableTitle`, `EditableSummary`, and `EditableListSection`.

The action function was generalized to build a PATCH body from whichever form fields are present (`title`, `summary`, `actionItems`, `decisions`, `openQuestions`).
Exported from Cursor View