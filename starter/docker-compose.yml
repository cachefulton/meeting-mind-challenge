services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: meetingmind
      POSTGRES_PASSWORD: meetingmind
      POSTGRES_DB: meetingmind
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U meetingmind"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
